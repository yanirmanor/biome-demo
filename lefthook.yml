pre-commit:
  parallel: true
  commands:
    check:
      glob: "./src/*.{js,ts,cjs,mjs,d.cts,d.mts,jsx,tsx,json,jsonc}"
      run: npm run biome:format
    run-tests:
      run: npm test
      glob: "./src/*.{js,ts}"
      exclude: "node_modules"
